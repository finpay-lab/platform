{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://finpay.example/contracts/events/v1/FundsReleased.json",
  "title": "FundsReleased",
  "description": "Published by wallet-service on the `finpay.wallet` topic when previously reserved funds are released (e.g. SAGA compensation). Partitioned by walletId.",
  "type": "object",
  "properties": {
    "eventId": {
      "type": "string",
      "format": "uuid",
      "description": "Globally unique event identifier. Consumers MUST deduplicate on this field (outbox redelivery / at-least-once semantics)."
    },
    "eventType": {
      "const": "FundsReleased",
      "description": "Fixed event type; matches the schema name and the stream-filterable discriminator."
    },
    "occurredAt": {
      "type": "string",
      "format": "date-time",
      "description": "UTC ISO-8601 timestamp when the event occurred."
    },
    "version": {
      "const": 1,
      "description": "Major schema version of this event. Additive changes stay at the same major; breaking changes require a new vN/ schema."
    },
    "partitionKey": {
      "type": "string",
      "description": "Kafka partition key; equals the business key walletId for ordering per wallet."
    },
    "payload": {
      "type": "object",
      "properties": {
        "walletId": {
          "type": "string",
          "format": "uuid",
          "description": "Wallet aggregate identifier."
        },
        "amount": {
          "type": "string",
          "format": "decimal",
          "description": "Released amount as a decimal string (no floating point)."
        },
        "currency": {
          "type": "string",
          "pattern": "^[A-Z]{3}$",
          "description": "ISO-4217 currency code of the released amount."
        },
        "reservationId": {
          "type": "string",
          "format": "uuid",
          "description": "Reservation reference being released."
        }
      },
      "required": ["walletId", "amount", "currency", "reservationId"]
    }
  },
  "required": ["eventId", "eventType", "occurredAt", "version", "partitionKey", "payload"],
  "examples": [
    {
      "eventId": "8d2e1f4a-5c6b-7d8e-9f0a-1b2c3d4e5f6a",
      "eventType": "FundsReleased",
      "occurredAt": "2026-08-12T06:32:18.000000Z",
      "version": 1,
      "partitionKey": "b0a9c8d7-e6f5-4321-0fed-cba987654321",
      "payload": {
        "walletId": "b0a9c8d7-e6f5-4321-0fed-cba987654321",
        "amount": "250.00",
        "currency": "EUR",
        "reservationId": "4e3d2c1b-0a9f-8e7d-6c5b-4a3f2e1d0c9b"
      }
    }
  ]
}
